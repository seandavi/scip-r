## Summary

<!-- What changes and why. Link the issue if there is one: Closes #123 -->

## Checklist

- [ ] Tests added or updated (`uv run pytest`)
- [ ] `uv run ruff check . && uv run ruff format --check . && uv run mypy` pass
- [ ] If parser output changed, the golden index was regenerated and the diff is explained above
- [ ] `CHANGELOG.md` updated under *Unreleased* (user-visible changes only)
